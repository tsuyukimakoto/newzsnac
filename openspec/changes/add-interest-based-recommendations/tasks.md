## 1. 保存形式と設定

- [x] 1.1 `item_embeddings`と`item_recommendations`、必要な索引を追加するSQLiteマイグレーションを実装し、既存データを保持した適用とバックアップ復元をテストする
- [x] 1.2 埋め込みモデルID、入力最大文字数、入力版、類似度閾値を起動設定へ追加し、ローカルLM Studio以外の接続先と範囲外の値を拒否する
- [x] 1.3 埋め込みモデルの設定状態と処理件数をランタイム状態へ追加し、未設定でもWeb、収集、分析を起動できることをテストする

## 2. 関心フィードバック

- [x] 2.1 記事一覧へ関心状態と関心絞り込みを追加し、保存・既読・興味なしから独立した三値の状態遷移を統合テストする
- [x] 2.2 `article.interest`操作をWeb API、CLI、外部制御へ接続し、入力検証、操作履歴、クライアント間の結果一致を契約テストする
- [x] 2.3 Web画面へ「気になった」ボタン、一覧の状態印、サイドバーの絞り込み、`i`ショートカットを追加する
- [x] 2.4 キーボードとポインターの両方で設定、解除、絞り込みができ、既読と保存が変化しないことをE2Eテストする

## 3. 埋め込み生成

- [x] 3.1 タイトル、要約、本文から版付きの埋め込み入力とハッシュを決定的に生成する処理を実装し、欠損欄、切り詰め、同一入力をテストする
- [x] 3.2 LM Studioの`/v1/embeddings`クライアントを実装し、有限値、次元数、空ベクトル、HTTPエラーを検証する
- [x] 3.3 `Float32Array`とSQLite BLOBの相互変換、次元数、L2ノルム、モデル互換性を検証する埋め込み保存処理を実装する
- [x] 3.4 収集済み記事と分析更新から冪等な`embedding`ジョブを登録し、保存済み入力の再利用とモデル変更時の段階的な再生成を実装する
- [x] 3.5 LM Studio停止、不正応答、モデル未設定を再試行待ちへ隔離し、同時に閲覧と関心変更が継続することを統合テストする

## 4. 類似推薦

- [x] 4.1 同じモデル、入力版、次元数のベクトルだけを受け付けるコサイン類似度計算を実装し、ゼロノルム、非有限値、次元不一致をテストする
- [x] 4.2 新しい記事の埋め込みと新しい関心記事から候補を増分計算し、未読、関心未指定、興味なし除外、閾値を適用して最高の根拠を保存する
- [x] 4.3 関心解除と同じトランザクションで古い根拠を無効化し、影響する候補だけを残りの関心記事から背景再計算する
- [x] 4.4 記事一覧へ推薦状態、根拠記事、類似度を追加し、「読むべきかも？」一覧をスコア、公開日時、記事IDで安定して並べる
- [x] 4.5 推薦記事の表示、既読、保存が関心入力を増やさず、明示的な「気になった」だけが後続推薦を変えることを統合テストする

## 5. 画面表示とシステム検証

- [x] 5.1 通常一覧と記事ペインへ「読むべきかも？」、根拠記事、類似度を読みやすく表示し、推薦一覧への導線を追加する
- [x] 5.2 固定ベクトルを返す偽LM Studioで、収集、埋め込み、関心設定、推薦表示、関心解除後の再計算までをE2Eテストする
- [x] 5.3 1万件の記事と現実的な関心記事数で増分推薦を測定し、一覧表示が既存の50ミリ秒目標を維持することを確認する
- [x] 5.4 埋め込みモデルの設定、初回生成、停止時の表示、閾値調整、再生成方法を利用手順へ記載する
- [x] 5.5 全テストとOpenSpecの厳格検証を実行し、通常起動でWeb、収集、分析、埋め込み、推薦更新が監督されることを確認する
